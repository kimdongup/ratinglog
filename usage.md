# 사용 설명서

## 권한

한 설치는 한 조직을 대상으로 합니다. 다른 조직은 별도 설치/DB를 사용하세요.

| 역할 | 범위 |
| --- | --- |
| 관리자 `admin` | 전체 원장·사용자·감사 기록, 편집·보관·복원. 본인 원장/작성 버전 승인 불가 |
| 작성자 `editor` | 본인 초안 작성·수정·제출·보관, 조직 승인 자료 열람 |
| 검토자 `reviewer` | 제출된 자료 검토·승인·반려, 본인 검토 이력 및 승인 자료 열람 |
| 열람자 `viewer` | 승인된 버전과 해당 첨부파일·내보내기 열람 |

작성 중인 새 버전은 기존 승인 버전을 덮어쓰지 않습니다. 열람자는 새 초안 대신 승인된 이전 버전을 봅니다. 보관 자료는 일반 목록 및 승인 조회에서 제외됩니다.

## 원장 등록과 승인

1. 관리자가 CLI 또는 사용자 관리 화면에서 계정을 발급합니다.
2. 작성자가 새 원장의 고유 코드와 제목, 종류·범주·인가정보·출처·정의·적용일을 입력합니다.
3. 문서형은 근거 첨부파일, 표형은 검증 가능한 CSV 수치표를 넣습니다. SQL 필드는 참고 문구로만 저장하며 실행하지 않습니다.
4. 초안을 저장하고 내용·수치·첨부파일을 확인합니다. 수정할 때 새 첨부파일을 선택하지 않으면 기존 자료를 유지합니다.
5. 검토 요청 후 검토자가 승인 또는 사유를 적어 반려합니다. 제출 중인 버전은 수정할 수 없습니다.
6. 반려되거나 승인된 내용을 수정하면 변경 사유와 함께 새 초안 버전이 생성됩니다. 다른 사람이 먼저 수정했다면 재조회해야 합니다.
7. 과거 버전 화면 및 비교 화면에서 메타데이터와 수치의 추가·삭제·변경을 확인합니다.

승인은 내부 검토 상태입니다. 외부 감독기관의 인가를 뜻하지 않습니다. 적용 종료일은 포함하며 비어 있으면 무기한입니다. 같은 코드의 승인 버전 적용기간이 겹치면 기준일에 유효한 승인 버전 중 가장 높은 버전 번호를 조회합니다.

## CSV 형식

UTF-8(또는 UTF-8 BOM), 쉼표 구분, 아래 헤더를 정확히 사용합니다. 최대 10,000행이며 빈 표는 허용하지 않습니다.

```csv
age,sex,smoking,duration,rate
30,F,non_smoker,0,0.0005
30,M,non_smoker,0,0.0007
31,F,non_smoker,0,0.0006
31,M,non_smoker,0,0.0008
```

- `age`: 0~120의 도달연령. `duration`: 0~120, 0은 경과기간 구분 없음.
- `sex`: `F`, `M`, `U`(구분 없음). `smoking`: `non_smoker`, `smoker`, `all`.
- 복합 키 `(age, sex, smoking, duration)`는 중복할 수 없습니다.
- `rate`: 유한한 비음수 십진수, 소수점 이하 최대 12자리. 단위가 `probability`이면 0~1, `percent`이면 0~100, `per_thousand`이면 0~1000.
- 비어 있는 값, NaN/Infinity, 잘못된 열, 단위 범위 초과는 행 번호와 함께 거절합니다.
- API의 `probability`는 입력 단위를 0~1로 정규화한 십진 문자열입니다. CSV는 입력 단위 값을 유지합니다.
- 특정 차원의 나이가 연속되지 않으면 경고합니다. 간격을 자동 보간하거나 누락값을 0으로 채우지 않습니다.

`examples/mortality.csv`는 화면과 가져오기를 확인하기 위한 합성 예제입니다. 문서형은 표 없이 ZIP/PDF/CSV 근거자료만 관리할 수 있습니다.

## 첨부파일

파일 한 개 최대 10 MiB, 전체 요청 최대 12 MiB입니다. PDF, UTF-8 CSV, ZIP만 허용합니다. ZIP은 압축을 풀지 않고 경로·심볼릭 링크·암호화·확장 크기·압축률을 검사합니다. 첨부파일은 인증·버전 권한을 확인한 뒤 다운로드 형태로만 제공합니다. SHA-256은 동일 바이트 확인용이며 악성코드 검출을 보장하지 않습니다.

## 승인표 API

브라우저 로그인 세션을 사용하는 읽기 전용 API입니다. 익명 호출은 401입니다. 장기 API 토큰·공개 API는 제공하지 않습니다.

- `GET /api/v1/rates`: 접근 가능한 비보관 원장의 승인 버전 목록. `page`, `q` 지원.
- `GET /api/v1/rates/<code>?as_of=2026-01-01`: 기준일에 유효한 승인표. 기준일 생략 시 UTC 오늘.
- `GET /api/v1/rates/<code>/lookup?age=30&sex=F&smoking=non_smoker&duration=0&as_of=2026-01-01`: 정확히 일치하는 한 행 조회. 차원 대체/보간 없음.

JSON에는 버전, 원문 단위, 정규화 확률, 출처·적용기간을 포함합니다. 없는 표·차원은 404, 잘못된 조회 조건은 400입니다. 표형이 아닌 자료의 수치 조회는 422입니다.

## 보관과 계정

보관은 자료를 숨기며 감사 기록과 과거 버전을 보존합니다. 관리자는 사유와 함께 복원할 수 있습니다. 계정 삭제 대신 비활성화하여 감사 기록의 주체를 유지합니다. 비밀번호·역할 변경, 비활성화와 로그아웃은 모든 기기의 기존 로그인 세션을 무효화합니다. 본인 비밀번호 변경에는 현재 비밀번호가 필요합니다.

## 제약

기관별 승인 체계, SSO/MFA, 이메일 비밀번호 복구, 실시간 보험료 산출, 노출량 기반 경험통계·생존분석, SOA XML/Excel 가져오기, 바이러스 검사 서비스는 후속 확장입니다. 본 앱의 CSV 검증은 데이터 형식 검증이며 계리적 적정성을 판정하지 않습니다.
